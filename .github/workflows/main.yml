name: General

on:
  push:
    branches: [ master, develop ]
jobs:
  postgresql:
    uses: douglas-dreer/pokemon-tcg-server/.github/workflows/postgresql.yml@1

  sonarcloud:
   needs: postgresql
   uses: douglas-dreer/pokemon-tcg-server/.github/workflows/sonarcloud.yml@1