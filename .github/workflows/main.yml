name: General

on:
  push:
    branches: [ master, develop ]
jobs:
  postgres_job:
    name: Postgresql
    runs-on: ubuntu-latest
    steps:
      - name: Setup Postgresql
        uses: harmon758/postgresql-action@v1
        with:
          postgreql version: '11'