name: General

on:
  push:
    branches: [ master, develop ]
jobs:
  postgres_job:
    name: Postgresql
    runs-on: ubuntu-latest
    steps:
      - name: Starting
        uses: .github/actions/postgresql@master
        with:
          branch: develop
          github_token: ${{ secrets.GITHUB_TOKEN }}